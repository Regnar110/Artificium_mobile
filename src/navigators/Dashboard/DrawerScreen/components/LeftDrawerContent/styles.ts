import { StyleSheet } from 'react-native';
import { COLORS } from '../../../../../infrastructure/enums';

export const styles = StyleSheet.create({
	leftDrawerContentContainer: {
		flex: 1,
		backgroundColor: COLORS.NOBLE_800,
		paddingHorizontal: 16,
		paddingVertical: 24,
		gap: 24
	}
});