import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
	fieldOuterContainer: {
		flexDirection: 'column',
	},

	fieldWrapper: {
		flexDirection: 'row',
		marginVertical: 8
	},
});